<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <OutputType>Exe</OutputType>
    <Nullable>enable</Nullable>
    <EmitCompilerGeneratedFiles>true</EmitCompilerGeneratedFiles>
  </PropertyGroup>

  <ItemGroup>
    <AdditionalFiles Include="../dbus-xml/org.freedesktop.NetworkManager_proxy.xml" Namespace="NetworkManager.DBus" DBusGeneratorMode="Proxy" />
  </ItemGroup>

  <!-- This Import is only needed because we're using a ProjectReference instead of a PackageReference for the source generator. -->
  <Import Project="../../src/Tmds2.DBus.Generator/Tmds2.DBus.Generator.props" />

  <ItemGroup>
    <ProjectReference Include="../../src/Tmds2.DBus.Protocol/Tmds2.DBus.Protocol.csproj" />
    <ProjectReference Include="../../src/Tmds2.DBus.Generator/Tmds2.DBus.Generator.csproj" OutputItemType="Analyzer" ReferenceOutputAssembly="false" />
  </ItemGroup>

</Project>
